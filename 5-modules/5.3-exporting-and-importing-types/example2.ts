// The types file is excluded from the compiled example2.js file
// since we're only importing a type from it.
import { Product } from "./types";

const product: Product = {
  name: "apple",
  price: 0.5,
};
